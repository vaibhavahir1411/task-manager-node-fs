<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <style>
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
}

.container {
    max-width: 800px;
    margin: 80px auto;
    padding: 30px;
    background-color: white;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

h2 {
    font-size: 28px;
    margin-bottom: 20px;
    color: #333;
    text-align: center;
}

textarea {
    width: 97%;
    padding: 12px;
    margin: 10px 0;
    border: 1px solid #ccc;
    border-radius: 8px;
    resize: none;
    font-size: 16px;
    color: #333;
}

.title-input {
    height: 40px;
}

.desc-input {
    height: 120px;
}

.save-button {
    background-color: #5a8d6a;
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    margin-top: 10px;
    margin-left: 40%;
    font-size: 16px;
}

.save-button:hover {
    background-color: #478057;
}

    </style>
</head>
<body>
    <div class="container">
        <h2>Edit Task</h2>
        <form action="/update/<%= filename %>" method="post">
            <textarea class="title-input" placeholder="Title" name="title"><%= filename %></textarea>
            <textarea class="desc-input" placeholder="Description" name="desc"><%= filedata %></textarea>
            <input class="button save-button" type="submit" value="Save Changes">
        </form>
    </div>
    
    

</body>
</html>
